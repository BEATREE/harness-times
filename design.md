# Harness Times · 设计系统

> 一份可以整体搬走的「报纸版式 Web 设计系统」。
>
> 这里沉淀的不是「本项目的样式表说明」，而是**为什么长这样、换个项目怎么复用**。
> 全部实现集中在 `src/styles/global.css`（3,005 行，按 23 个分节组织），
> 组件级样式在各自的 `.astro` 文件里（Astro 会自动 scoped）。
>
> 版本：2026-09 · 对应当前线上 `https://harness.beatree.cn`（Pages 默认域名 `harness-times.pages.dev` 同样可用）

---

## 一、四条设计原则

**① 报纸的版式语言，不要报纸的出版制度。**
报头、三线表、首字下沉、双栏线、pull quote、版块编号（I / II / III / IV）——这些是**版式**，
全部保留，因为它们给长文提供「一眼看出结构」的锚点。
但期号、「本报」「第 N 期」这些是**出版制度**，一律不要：本站是按章组织、随修订更新的。

**② 纸是底色，不是装饰。**
整站没有一张背景图片、一个渐变装饰块。层次完全靠**纸的三级深浅**
（`--paper` / `--paper-dark` / `--paper-light`）+ **两级描边** + **一层浅阴影**做出来。
好处是：任何一块组件单独截图都不突兀，拼在一起自动协调。

**③ 可读性优先于对称。**
正文栏宽只有一条底线：**一行放得下约 30 个汉字**（≥420px），
够宽时直奔「和 `.chapter-toolbar` 同宽」。
当视口不足以同时满足「正中央」和「栏宽达标」时，**牺牲居中，保可读**——
详见第五节的镜像让位存废，这是这套版面里最需要理解的一处取舍：
1300px 以上宁可让正文相对屏幕偏右 134px，也要把正文从 604px 撑到 809px。

**④ 动效只用来表达「顺序」和「方向」，不用来炫技。**
两种动效，各有明确语义：**错峰淡入**表达「读的顺序」，**流向彗星**表达「数据往哪走」。
时长控制在 0.12–0.26s，唯一的长动画（图解扫读 7s）是持续的低强度呼吸，不是一次性表演。

---

## 二、色板

全部色值定义在 `:root`，共三组 13 个 + 4 个领域色。**其它地方只引用变量，不写色值。**

### 纸与墨（定调的一层）

| 变量 | 值 | 用途 |
| --- | --- | --- |
| `--paper` | `#f4efe6` | **全局底色**。`html` 与 `body` 都用它——这一条同时解决「换页闪白」和「灰屏过渡」 |
| `--paper-light` | `#faf7f2` | 抬起的surface：卡片、悬浮控件、代码块名栏 |
| `--paper-dark` | `#e8e0d4` | 压下的surface：表头、次级底板 |
| `--paper-deep` | `#ded4c4` | 最深一级：行内 code 底、禁用态 |
| `--ink` | `#1a1816` | 标题、强调正文 |
| `--ink-soft` | `#4a4540` | **正文默认色**（`--ink` 直接做正文会偏硬） |
| `--ink-muted` | `#8a8580` | 元信息、标签、说明 |
| `--ink-faint` | `#b3ada4` | 分隔符、占位、极次要 |

### 套色（强调色，各司其职）

| 变量 | 值 | 语义 | 出现位置 |
| --- | --- | --- | --- |
| `--red` | `#9b2c2c` | **主强调**（报纸朱红） | 链接、Harness 领域、警告、当前项 |
| `--red-soft` | `#c05a5a` | 主强调的浅版 | 悬浮、次级标记 |
| `--gold` | `#b8944b` | **排行 / 标记** | 版块编号、pull quote 竖线、评测领域 |
| `--gold-soft` | `#d9c08a` | 金色浅版 | 外链项底色 |
| `--teal` | `#2f6157` | **正面 / 数据** | 知识引擎领域、成功态、数据流 |
| `--blue` | `#2c4a7c` | **原理 / 模型** | 大模型原理领域 |

> **套色的纪律**：每个领域**只用一个**颜色，且在侧栏、领域页、图解、章节强调里保持同一个。
> 读久了形成条件反射：看到 `#9b2c2c` 就知道在讲 Harness。

### 线

| 变量 | 值 | 用途 |
| --- | --- | --- |
| `--border-light` | `#e4dcd0` | 最轻的分隔（列表项之间） |
| `--border` | `#d4cbbc` | 常规边框 |
| `--border-strong` | `#a89e8d` | 需要「硬一点」的边框（输入框、强调框） |

报纸的线都是**暖灰**，不是中性灰。这是整套色板看起来「像纸」的关键之一。

### 阴影（克制到几乎没有）

```css
--shadow:      rgba(26, 24, 22, 0.08);   /* 常规抬起 */
--shadow-deep: rgba(26, 24, 22, 0.16);   /* 抽屉、主站 CTA */
```

阴影用**墨色**（`#1a1816`）而不是纯黑，且透明度很低。纸面上不该有塑料感。

### 领域色（与套色同源，用于侧栏分组与图解）

| 领域 | 主色 | 浅底 | 深字 |
| --- | --- | --- | --- |
| `llm` 大模型原理 | `#2c4a7c` | `#eef1f7` | `#2c4a7c` |
| `harness` Harness 工程 | `#9b2c2c` | `#fbf1f1` | `#9b2c2c` |
| `eval` 评测工程 | `#b8944b` | `#fdf6e8` | `#8a6a1e` |
| `knowledge` 知识引擎 | `#2f6157` | `#eef4f1` | `#2f6157` |

侧栏用 `--dc` / `--dc-bg` 两个变量承接，**只在不领域分组上定义一次**，
内部所有子元素引用变量——这样加领域不用改任何子组件样式。

---

## 三、字体系统

### 只使用系统字体栈

**不请求任何字体 CDN。** 理由：Google Fonts 在国内访问受限，且会阻塞首屏渲染。
拉丁字形靠系统自带的 Playfair Display / Georgia（macOS、Windows 都有）达成报纸味。

### 四条栈 + 明确分工

| 变量 | 主要用途 | 关键字形 |
| --- | --- | --- |
| `--font-display` | 标题、报头、版块名 | 拉丁**衬线**（Playfair Display → Georgia → Times） |
| `--font-body` | 整段正文 | 全**黑体**（system-ui → PingFang SC → 微软雅黑） |
| `--font-ui` | 按钮、标签、元信息 | 无衬线（DM Sans → system-ui） |
| `--font-mono` | 代码、图解内标注 | 等宽（JetBrains Mono → ui-monospace → Consolas） |

**这套分工是整套设计里最「反直觉但正确」的一条：**

- 标题的**拉丁走衬线、中文走黑体**——中文报纸的标题本来就用黑体（宋体是正文用的），
  而且 Windows 上基本装不到思源宋体，落到 `SimSun` 会有一圈明显毛边。
  混排时英文的衬线感足够撑住「报头」的气质，中文用黑体反而更精神。
- 正文**全走黑体**，一个衬线回退都不留。曾经的衬线正文在 Windows 上只能落到 `SimSun`，
  小字号明显发虚——这是「字看着脏」的根因，不是字号问题。
- `--font-mono` **不混入任何中文字体**。混入后同一行里的中英文基线会飘。

> ⛔ **禁止**：在任何 `font-family` 里加 `SimSun` / `宋体`。`verify-build.mjs` 有断言盯着。

### 字号阶梯（rem，根 16px）

| 层级 | 值 | 字重 | 说明 |
| --- | --- | --- | --- |
| 报头主标题 | `3.4rem` | 700 | 仅头版 |
| 章标题 H1 | `2rem`+ | 700 | `--font-display` |
| 正文基准 | **`1.0625rem`**（17px） | 400 | `line-height: 1.86`，`text-align: justify` |
| 社论体（整段强调） | `1.09rem` | 600 | 左金线 + 略大 |
| 小节标题 H2 | `1.3rem` 左右 | 700 | |
| 列表 / 表格 | `0.98rem` | 400 | |
| 界面控件 | `0.9rem` | 500 | |
| 元信息 / 标签 | `0.6rem`–`0.7rem` | 500 | 常配 `letter-spacing: 0.16em`+ |
| 全大写小标签 | `0.54rem`–`0.62rem` | 500 | `text-transform: uppercase` |

**正文 17px / 1.86** 是这个站读起来不累的核心参数。
行高按中文调整过：中文没有词间距，行高比拉丁正文要更松。

`text-align: justify` + `text-justify: inter-ideograph` 让中文两端对齐，
但**不**用在代码、表格、标签上（会造成字间距撕裂）。

### 全局字号保护

```css
html { -webkit-text-size-adjust: 100%; text-size-adjust: 100%; }
```

关掉移动端浏览器的字号自动放大。否则同一段落在不同机型上字重不一致，
精心调的行高会失效。

---

## 四、尺度变量

```css
--sidebar-w: 268px;      /* 侧栏宽度；≤980px 时降到 250px */
--sidebar-hold: var(--sidebar-w);  /* 版面为目录预留的横向留白，见第五节 */
--sheet-pad: 56px;       /* 纸面左右内边距；正文/章节头/章尾与工具栏同宽的唯一来源 */
--rail-w: 150px;         /* 两侧大翻页区宽度；≥1700px→200px，≥2000px→250px */
--rail-gap: 24px;        /* 翻页区与纸面之间的间隙 */
--measure: 40rem;        /* 只有「没有翻页区的窄屏」还用它限制正文栏宽 */
--gap: 32px;             /* 常规间距基准 */
```

间距约定：`4 / 8 / 12 / 16 / 22 / 32 / 40 / 56 / 72`（px），
不成体系的值不要引入。组件内边距常用 `padding: 40px var(--sheet-pad) 72px`（`.sheet`）。

> `--sheet-pad` 是「正文与 `.chapter-toolbar` 同宽」这条要求的实现方式：
> 工具栏用负 margin 撑满整张纸，正文用等量负 margin 跟着撑满，
> 两条边线因此必然重合。**改内边距只改这一个变量**——
> 一旦有人只改了其中一处，`npm run verify:build` 与 `node scripts/measure.mjs` 会报红。

---

## 五、版面骨架 ★ 最需要理解的一节

版面有两个形态，以 **1300px** 为界。

**A · `< 1300px`：单栏纸面**

```
html / body          background: var(--paper)      ← 纸色底，防闪白与灰屏
└── .app              display: flex
    ├── .sidebar      position: fixed; width: var(--sidebar-w)   ← 脱离文档流！
    └── .main         flex: 1; min-width: 0
        └── .sheet    max-width: 1120px; margin: 0 auto
            ├── 章节头      max-width: var(--measure); margin-left/right: auto
            ├── .prose      同上
            └── 章尾       同上
```

**B · `≥ 1300px`：正文两侧各挂一块大翻页区**

```
.main                     display: flex; justify-content: center; gap: var(--rail-gap)
├── a.page-rail.rail-prev flex: 0 0 var(--rail-w); position: sticky; top: 96px; min-height: 300px
├── .sheet                flex: 1 1 auto; min-width: 0; max-width: 1120px
│   ├── .chapter-toolbar  width: calc(100% + 2*var(--sheet-pad)); margin: 0 -var(--sheet-pad)
│   ├── 章节头 / .prose / 章尾   ← 与工具栏用同一式子，所以同宽
│   └── …
└── a.page-rail.rail-next 同上（末章变成指向 /progress/ 的「全书终点」）
```

**翻页区必须是 `.sheet` 的兄弟节点，不能是子节点**——三栏 flex 的前提是它占据
「正文纸面之外」的空间。这就是它走 `BaseLayout` 具名 slot（`rail-prev` / `rail-next`）
而不是写在 `ChapterLayout` 内部的原因；写进默认 slot 会让三栏布局静默塌成单栏。

### 5.1 为什么需要 `--sidebar-hold`

侧栏是 `position: fixed`，**不占文档流**。所以 `.main` 默认会从视口最左边开始，
正文就会被侧栏压住。

```css
.main {
  flex: 1;
  min-width: 0;                              /* 关键，见 5.3 */
  margin-left: var(--sidebar-hold);          /* 给目录让位 */
  padding-right: min(var(--sidebar-hold),    /* 镜像让位，见 5.2 */
                     max(0px, calc(100% - 860px)));
}

@media (min-width: 1300px) {
  .main { padding-right: 0; }                /* 镜像让位在这里被取消，见 5.2 */
}
```

### 5.2 镜像让位的存废：一个明确的取舍

两份相等的留白 → `.main` 的内容盒**以视口中线为中心** → 正文 `margin: auto` 居中
→ **中点落在屏幕上**。这是「正文严格居中」的实现。

但 `268 + 268 = 536px` 是白送出去的。768px 的平板上：

```
768 − 268（侧栏） − 268（镜像） = 232px  → 减去 padding 只剩 185px，一行不到 11 个字
```

所以右边那份取 `min(镜像, 剩余空间)`，以「正文栏不小于 ~480px（约 30 字）」为界。

**到了 1300px 以上，镜像让位被彻底取消**（`padding-right: 0`）。原因：
镜像留白要 268px，两侧翻页区又要 `(150+24)×2 = 348px`，两笔加起来在 1440px 视口里
会把正文栏压到 604px —— 比不加翻页区时还窄，与「正文栏过窄」的诉求正面冲突。
所以这一版把空间还给正文与翻页区。

实测（`node scripts/measure.mjs`，视口 1425px = 1440 减滚动条）：

| 视口宽度 | 正文栏 | 正文中点 vs 视口中线 | 状态 |
| --- | --- | --- | --- |
| 1440px（展开侧栏） | **761px**（= 工具栏宽） | **+134px** | 翻页区在，居中基准是 `.main` 内容区 |
| 1440px（收起侧栏） | **1077px**（= 工具栏宽） | +0.5px | 侧栏归零，正文自然回到视口中线 |
| 1280px | 617px | +0.5px | 镜像留白留满，**严格居中** |
| 1024px | 480px | +59px | 镜像收缩，略右漂 |
| 768px | 435px | +125px | 同上，守住 420px 可读下限 |
| 390px | 350px | 0px | 抽屉式侧栏，两侧归零 |

`.main` 在 ≥1300px 时左右各留 `--rail-gap`（24px）的呼吸位。
这一份**不能省**：试过 `padding-right: 0`，结果是右侧翻页区正好贴在屏幕最右缘，
1px 描边 + 2px 圆角看起来像被裁掉。因为左右等量，加它不改变正文的居中基准（仍偏右 134px），
只把正文栏从 809px 收窄到 761px——**24px 换一个不贴边的面板，值**。
`tools/verify.mjs` 里有「左右翻页区都留了呼吸位（不是贴边）」这条断言守着它。

**这是两条明确的取舍，不是 bug：**

1. **≥1300px 时正文不再「视口居中」，而是「`.main` 内容区居中」。**
   因为有固定侧栏，内容区的中心比视口中线右偏 134px（= 侧栏宽 / 2）。
   换来的是正文栏从 604px 涨到 809px（+34%）以及 150×315 的可点翻页面板。
   一句话：**正文更宽、翻页区更大，但整组内容相对屏幕略偏右。**
2. **< 1128px 时优先牺牲「居中」而不是「能读」。**
   如果哪天有人要求「任何宽度都必须严格居中」，请先确认 768px 下正文栏宽能接受降到 185px。

### 5.3 `min-width: 0` 不能省

flex 项的 `min-width` 默认是 `auto`，等于「min-content 宽度」。
章节正文里有 `white-space: pre` 的代码块，其 min-content 可能长达 700px+，
于是 `.main` 会被撑到 725px，**在 390px 的手机上整页横向溢出**。

显式置 0 后 `.main` 可以收缩到容器宽度，超长代码行由 `.code-block pre` 的
`overflow-x: auto` 自己滚动。

`.sheet` 在 ≥1300px 变成 flex 项，同样要 `min-width: 0`——否则它会被正文里的长代码
顶得比 `--rail-w` 留给它的空间还宽，把翻页区挤出视口。

### 5.4 侧栏折叠时的行为（相对上一版已改变）

```css
@media (min-width: 761px) {
  .main { transition: margin-left 0.26s …, padding-right 0.26s …; }
  body.nav-collapsed { --sidebar-hold: 0px; }   /* 两侧同时归零 */
}
```

**上一版**靠镜像留白把正文钉在视口中线上，所以折叠侧栏时正文一格不动
（当时的不变性断言是 `|位移| ≤ 1px`）。**取消镜像留白之后这条不再成立**：
`--sidebar-hold` 归零 → `.main` 占满视口 → 正文左边界左移 268px、宽度 809 → 1077px。

这是**主动改的语义**：收起侧栏 = 给内容更多空间，而不是「侧栏消失但正文不动」。
`scripts/measure.mjs` 的不变性断言也随之改了，现在它保证的是两件真正该成立的事：

- 收起后正文**不会更窄**（腾出的空间确实被内容吸收了，而不是白白扔掉）；
- 收起后正文**没有跑到屏幕外**（左边界 ≥ 0）。

---

## 六、组件清单

样式集中在 `global.css` 的 22 个分节里。核心组件与它们的「设计意图」：

| 组件 | 关键类 | 设计意图 |
| --- | --- | --- |
| 报头 | `.masthead` · `.masthead-top` · `.made-by` · `.site-link` | 双线（`3px double`）压顶，模拟报纸刊头；出品方 + 主站 pill 放右上 |
| 版块标题 | `.section-head` | 中文标题 + 拉丁副标 + 一段细线，分隔大块 |
| 正文排版 | `.prose` | 有翻页区时与 `.chapter-toolbar` 同宽（共用 `--sheet-pad`）；无翻页区的窄屏才回到 `--measure`；`h2` 带编号；`em:only-child` 变社论体 |
| 首字下沉 | `.dropcap` | 每章导语段，报纸标志性手法 |
| 信息盒 | `.box` · `.box-key` · `.box-practice` · `.box-warn` | 三色语义：金=关键、青=实操、红=警告。**不要混用** |
| 三线表 | `.tbl-wrap` > `table.news` | 上下粗线 + 表头下细线；`.tbl-wrap` 负责窄屏横向滚动 |
| 插图 | `figure.fig` · `.fig-frame` · `figcaption` | 见第七节 |
| 代码 | `.code-block` · `.code-head` · `.lang` | 文件名栏是识别点；Shiki 高亮 |
| 面试问答 | `.qa` | 问题 + 答案 + 加分点 + 追问链，可折叠 |
| 自测题 | `.quiz` · `.q-item` · `.opt` · `.explain` | 四选一，答后展开解析 |
| 章节头 | `.chapter-head` | 版块号 + 领域标签 + 难度 + 时长 + 导语 |
| 章节工具条 | `.chapter-toolbar` | 进度 / 笔记 / 自评，滚动时吸顶 |
| 侧边翻页 | `.page-rail` · `.rail-prev` · `.rail-next` · `.rail-k` · `.rail-title` · `.rail-desc` · `.rail-meta` · `.rail-hint` | 见第八节 |
| 出口区（关于页） | `.cta-row` · `.cta` · `.cta-main` · `.cta-sub` · `.cta-slim` · `.cta-src` | 两块主推竖卡（主站 `1.4fr` / 公众号 `1fr`）+ 一条整宽源码横条。**竖卡不要做第三张**：侧栏占位后纸面只有 600～700px，三并排会把 `.cta-d` 说明压成「一列一个字」；横条对纸面宽度不敏感，`flex-wrap` 自己就够，不需要断点 |
| 目录卡片 | `.card` | 首页与领域页的章节入口 |
| 进度页 | `.stat-row` · 热力图 | 本机数据面板 |
| 页脚 | `.site-foot` | |
| 提示条 | `.notice` | |

---

## 七、插图（图解）设计规范

图解是这个站的识别度来源，也是**踩坑最多**的地方。完整规则在 `wiki/schema.md` 第三节，
这里只讲设计决策：

### 7.1 为什么是手写 SVG，不是图片

- 手写 SVG 是**文本**：能过构建管线、能吃 CSS 变量做动效、能随主题改色、
  能带 `aria-label` 被读屏软件读出来。位图这些都做不到。
- 代价是必须守两条铁律：**HTML 块内不留空行**、**空元素必须自闭合**。
  这两条都会导致「整张图静默消失」，所以都有构建期守卫（见 `wiki/schema.md` 3.3）。

### 7.2 图内的视觉语言

| 项 | 取值 |
| --- | --- |
| 标题字体 / 正文字体 | `Georgia, serif` / `ui-monospace, monospace` |
| 字号 | `8.2` – `13`，标题 `11`–`13`，说明 `8.7`–`9.8`，**下限 8.2** |
| 墨色 / 灰褐 | `#1f1b16` / `#6b6257` |
| 节点描边 | `stroke-width="1.3"`；连线 `1.2` |
| 强调色 | 取自所属领域色（见第二节） |
| 箭头 | `<defs><marker>`，`id` 同页内唯一 |

### 7.3 移动端：不缩，改成横向滚动

设计稿宽 660px，手机正文栏约 340px。等比缩放会让图里 9px 的字变成 4.7px，**读不了**。
所以**不让它缩**：

```css
figure.fig svg { min-width: calc(var(--vbw, 660) * 1px); }        /* 基础 */
@media (max-width: 1128px) { svg { min-width: calc(var(--vbw) * 1.35px); } }  /* 窄屏还放大一点 */
@media (max-width: 1128px) and (hover: none) { /* 追加「↔ 图解可左右滑动」提示 */ }
```

`--vbw` 由构建期插件从 `viewBox` 宽度自动写入。
**继承关系：`viewBox` 缺失 → 移动端图解不可读**（同一个 bug 的两种表现）。

### 7.4 动效：让图「自己被读出来」

| class | 加在哪 | 表现 | 语义 |
| --- | --- | --- | --- |
| `dm-t` | 所有 `<text>` | 按 `--i` 错峰淡入，每级 22ms，上限 760ms | **读的顺序** |
| `dm-n` | 有描边的 `rect`/`circle` | `--sw` 驱动的描边宽度脉冲（7s 呼吸） | **层的顺序** |
| `dm-plate` | 无描边底板 | 同上，弱化版 | 底板跟进 |
| `dm-go` | 箭头线 | `--len` 驱动的 `stroke-dashoffset` 彗星 | **数据流向** |
| `dm-svg` | `<svg>` | 容器钩子 + 写入 `--vbw` | — |

全部由 `src/lib/diagram-motion.mjs` 在构建期标注，**正文里不要手写这些 class**。
纯 CSS，无 JS，所以**没有首帧闪烁**（JS 方案会在 hydration 前先渲染一帧无动效状态）。

`prefers-reduced-motion: reduce` 下全部关闭、彗星层 `opacity: 0`。

---

## 八、两侧大翻页区（`.page-rail`）

正文左右两侧的「上一章 / 下一章」。设计上有四个决策：

**① 只在 ≥1300px 显示。**

1300px 是算出来的，最紧的一档账：

```
侧栏 268 + 两个翻页区 (150+24)×2 = 348 + 正文栏可读下限 720 ≈ 1336px
```

宁可不给翻页区，也不把正文压到不好读。窄屏下翻页区整体退场，
翻页能力交给章尾的 `nav.pager`（它一直都在，不是新增的降级方案）。

```css
.page-rail { display: none; }
@media (min-width: 1300px) { .page-rail { display: flex; } }
```

**② 是「整块可点的大面板」，不是小书签。**

上一版是 34px 宽的悬浮小书签，两个问题：点击热区太小；
看不出「这里能翻页」。现在是 **150×(≥300)px 的整块面板**，
宽高都由 `--rail-w` 与 `min-height` 锁住，`scripts/measure.mjs` 会断言这两个数字——
只断言 `display` 是不够的，小书签同样 `display: flex`，那样断言照样绿。

**③ 面板内容是可读文本，不是装饰。**

面板上直接显示目标章节的**标题、摘要、版块与时长**，用户不必悬浮就能决定要不要点进去。
键盘提示（`.rail-hint`）与方向箭头（`.rail-chev`）才是 `aria-hidden="true"` 的装饰，
可访问名走 `aria-label`（如「上一章：<章节标题>」）——
否则读屏会把「上一章 + 标题 + 摘要 + 元信息 + 键盘提示」全念一遍。

> 注意 `.rail-hint` 的颜色用的是 `--ink-muted`（3.19:1）而不是 `--ink-faint`（1.94:1）。
> 提示「还有另一种操作方式」是有用信息，不是分隔符，不能落在 11.3 里登记的那类缺口上。

**④ 键盘 ← / → 与面板等价，但有三道不抢键的守卫。**

```js
document.addEventListener('keydown', (e) => {
  if (e.key !== 'ArrowLeft' && e.key !== 'ArrowRight') return;
  if (e.metaKey || e.ctrlKey || e.altKey || e.shiftKey) return;   // 守卫 2
  if (isTyping(document.activeElement)) return;                    // 守卫 1
  if (active.scrollWidth > active.clientWidth + 1) return;         // 守卫 3
  (e.key === 'ArrowLeft' ? prev : next)?.click();
});
```

| 守卫 | 防的是什么 | 不设会怎样 |
| --- | --- | --- |
| 1 · 焦点在 `INPUT/TEXTAREA/SELECT/contentEditable` | **本章页面里就有笔记输入框** | 在输入框里按 ← 移动光标，每按一次就跳走一章 |
| 2 · 按了修饰键 | `Cmd/Alt + ←` 是浏览器前进后退 | 抢走浏览器自己的快捷键 |
| 3 · 焦点元素可横向滚动 | 表格 / 图解在小屏要横向滚动 | 用方向键看被藏住的内容时会突然跳章 |

跳转走 `target.click()` 而不是 `location.href`——
站内的换页过渡是拦 click 实现的，直接改 `location` 会让过渡失效。

末章的「下一章」变成 **`rail-next.is-end`**，指向 `/progress/`，
并换上金色顶边以区别于普通「下一章」——**不要让读者在最后一章走到死路**。

---

## 九、动效系统

### 曲线与时长

| 用途 | 时长 | 曲线 |
| --- | --- | --- |
| 悬浮 / 颜色变化 | `0.18s` | `ease` |
| 元素出现 / 位移 | `0.2s`–`0.24s` | `cubic-bezier(0.16, 1, 0.3, 1)` ← **主力曲线** |
| 版面重排（侧栏折叠、正文让位） | `0.26s` | `cubic-bezier(0.16, 1, 0.3, 1)` |
| 换页离场 | `0.18s` | `cubic-bezier(0.4, 0, 1, 1)`（加速，干脆地走） |
| 换页入场 | `0.26s` | `cubic-bezier(0.16, 1, 0.3, 1)`（减速，稳稳地来） |
| 图解扫读呼吸 | `7s` | 循环，低强度 |

`cubic-bezier(0.16, 1, 0.3, 1)` 用在全站 10 处——这是「快出慢入」的减速曲线，
动起来像有质量的纸，而不是弹性的果冻。

### 换页过渡：从「翻纸」到「淡入淡出」

```css
body.is-leaving .main { animation: paperExit 0.18s …; }  /* opacity 1→0, translateY 0→-8px */
body.is-entering .main { animation: paperEnter 0.26s …; } /* opacity 0→1, translateY 10px→0 */
```

**两个曾经踩过的坑，改法比效果重要：**

1. **位移收敛到 ≤10px、去掉旋转。** 旧版是 `translateX(±42px) + rotate(±0.9deg) + scale(0.985)`，
   幅度太大像在「甩」页面。
2. **底衬必须是纸色。** 旧版 `body` 背景是深炭色 `#2c2824`，而换页动画会先把 `.main`
   淡到全透明——**底下那层深色就整块露出来了，观感是一屏灰**。
   这是「灰屏过渡」的真正成因，不是动画参数问题。改 `body` 背景为纸色后，淡出即融入纸面。

> 教训：**做淡出动画前，先确认淡出后露出的是什么。** 透明不是「什么都没有」。

时长也压短了：MPA 的过渡是「先等旧页演完，再真的跳转」，
动画时长会实打实叠加成用户的等待，越长越像卡住。

---

## 十、响应式断点

| 断点 | 变化 | 取舍 |
| --- | --- | --- |
| `≥ 2000px` | `--rail-w` 150 → **250px** | 大屏把翻页区做大，正文不至于拉得过长 |
| `≥ 1700px` | `--rail-w` 150 → **200px** | 同上 |
| **`≥ 1300px`** | `.main` 变**三栏 flex**：出现左右大翻页区（150×≥300px）；**取消镜像留白**；正文用负 margin 与 `.chapter-toolbar` 同宽 | 再窄正文栏会被压到不好读；代价是正文不再视口居中（见 5.2） |
| `≥ 1128px` | 正文**严格居视口中线**（镜像留白留满，仅限 <1300px） | 低于此值居中开始让步 |
| `1128 – 768px` | 图解**放大 1.35×** + 右缘滚动阴影 | 宁可横向滚动，不让字号缩到读不了 |
| `≤ 980px` | `--sidebar-w` 268 → 250px；`--sheet-pad` 56 → **34px** | 挤一点，先把栏宽留住 |
| `≤ 900px` | 头版 / 目录卡片转单列 | — |
| `≤ 780px` | 正文相关的小结构调整 | — |
| `≤ 620px` | 关于页出口区：两块竖卡转单列；源码横条由 `flex` 转 `block` | 一行放不下「标签 + GitHub + 说明」，说明整行落下比挤成两截好读 |
| **`≤ 760px`** | **侧栏变覆盖式抽屉**（`☰` + `.scrim`）；两侧留白归零；隐藏折叠按钮与翻页区；`--sheet-pad` → **20px**，`.sheet` padding → `66px 20px 54px` | 移动端不占版面，正文满宽 |
| `print` | 隐藏侧栏 / 工具条 / **翻页区** / 页脚；`.main` 回退 `display: block` 并取消 `padding-right`；`.sheet` 与 `.prose` 全宽；链接去下划线；`.box` `.quiz` `figure.fig` `.qa` 设 `break-inside: avoid` | 打印时不要被切断的块 |

**窄屏下 `--sidebar-hold: 0px` 是统一开关**——因为抽屉不占版面。
如果将来改成常驻窄条，这一处和 `min-width: 761px` 那段要一起改。

`.chapter-head` / `.prose` / `.chapter-foot` 在三档里各有一套宽度规则，改 `--sheet-pad` 时
**这三处 + `.chapter-toolbar` 是同一组**，`npm run verify:build` 有对应断言把它们锁在一起。

---

## 十一、无障碍

### 11.1 已做到

| 项 | 做法 |
| --- | --- |
| 动效 | 所有动画（图解扫读、换页过渡、`scroll-behavior`、侧栏抽屉）在 `prefers-reduced-motion: reduce` 下关闭 |
| 图解 | 每个 `<svg>` 必须 `role="img"` + `aria-label`（图内 `<text>` 对读屏不构成可读内容，没有 `aria-label` 的图对屏幕阅读器等于不存在） |
| 装饰元素 | 纯装饰的字符与容器（`↗`、`.rail-chev`、`.rail-hint`）一律 `aria-hidden="true"` |
| 可访问名 | 靠 `aria-label` 提供，**不依赖装饰文本**（否则读屏会把「上一章 + 标题 + 摘要 + 元信息 + 键盘提示」全念一遍） |
| 键盘 | 所有交互元素用真实 `<a>` / `<button>`，不靠 `div` + click；章节切换另提供 ← / →，并有三道不抢键守卫（见第八节） |
| 字号 | `text-size-adjust: 100%` 关闭移动端自动放大；正文基准 17px，不用更小 |
| 长文 | `scroll-padding-top: 24px`，锚点跳转时标题不会被吸顶工具条盖住 |

### 11.2 对比度实测（底色 `--paper #f4efe6`）

用 WCAG 2.1 相对亮度公式实算的结果：

| 令牌 | 对比度 | 判定 |
| --- | --- | --- |
| `--ink #1a1816` | **15.46 : 1** | AAA ✅ |
| `--ink-soft #4a4540`（正文默认色） | **8.27 : 1** | AAA ✅ |
| `--blue #2c4a7c` | **7.71 : 1** | AAA ✅ |
| `--red #9b2c2c`（链接） | **6.57 : 1** | AA ✅ |
| `--teal #2f6157` | **6.18 : 1** | AA ✅ |
| `#8a6a1e`（深金，仅用于图解内文字） | **4.41 : 1**（金浅底上 4.69） | AA ✅ |
| `--gold #b8944b` | **2.49 : 1** | ❌ 小字不达标 |
| `--ink-muted #8a8580` | **3.19 : 1** | ⚠️ 大字号达标，小字不达标 |
| `--ink-faint #b3ada4` | **1.94 : 1** | ❌ 不达标 |

### 11.3 三个已知缺口（登记不掩盖）

| # | 缺口 | 影响 | 建议 |
| --- | --- | --- | --- |
| 1 | `--ink-muted` 只有 3.19:1，却被用在 `0.6rem` 级的元信息 / 标签上 | 弱视用户在极小字号下读不到元信息 | 元信息正文改用 `--ink-soft`；`--ink-muted` 只留给 ≥ 大字号（18.66px bold / 24px）或纯装饰 |
| 2 | `--ink-faint` 只有 1.94:1，但有 18 处当作文字色用，其中**包含交互控件** `.nav-collapse-btn`（侧栏折叠按钮） | 折叠按钮在浅色纸面上几乎看不见 | 控件与信息文字改用 `--ink-muted` 以上；`--ink-faint` 严格限制为「非信息性分隔符」 |
| 3 | `--gold` 2.49:1 被用来做版块编号（`I / II / III / IV`）的文字色 | 版块编号是导航信息，不是装饰 | 文字改用 `#8a6a1e`（4.41:1，已在图解里使用）；`--gold` 保留作线条与边框 |

这三条都**不属于「设计风格」问题，而是「不知道」**——写出实算数字比写「符合无障碍标准」有用得多。
若要收紧，改动点都在 `global.css` 的色值引用处，不涉及版式。

### 11.4 尚未做

- **无自定义 `:focus-visible` 样式**，目前依赖浏览器默认 `outline`。
  在纸色底上默认蓝色 outline 是清楚的，但没有和套色体系协调。
- **无自动化对比度审计**。本文的表格是手工实算 + 人工核对用途得出的，
  没有接 CI。若要接，建议加到 `scripts/verify-build.mjs` 里作为一组静态断言
  （扫描 CSS 里被用作 `color` 的令牌 + 所在字号，与 `--paper` 求对比度）。
- **未做键盘焦点顺序审计**，也未测试屏幕阅读器实读效果（只做了 ARIA 标注层面的保证）。

---

## 十二、怎么把这套设计搬到新项目

### 最小可复用骨架

```css
:root {
  /* 1. 纸与墨 —— 换这 8 个值就能整体换皮 */
  --paper: #f4efe6;  --paper-light: #faf7f2;  --paper-dark: #e8e0d4;  --paper-deep: #ded4c4;
  --ink: #1a1816;    --ink-soft: #4a4540;     --ink-muted: #8a8580;   --ink-faint: #b3ada4;
  /* 2. 套色 —— 按项目语义重命名，但保持「一色一义」 */
  --red: #9b2c2c;    --gold: #b8944b;         --teal: #2f6157;        --blue: #2c4a7c;
  /* 3. 线（暖灰，不要中性灰） */
  --border-light: #e4dcd0; --border: #d4cbbc; --border-strong: #a89e8d;
  /* 4. 尺度 */
  --sidebar-w: 268px; --sidebar-hold: var(--sidebar-w); --gap: 32px;
  --sheet-pad: 56px;  --rail-w: 150px; --rail-gap: 24px; --measure: 40rem;
}
html, body { background: var(--paper); }        /* ← 这一条决定「不闪白、不灰屏」 */
body { font-family: var(--font-body); color: var(--ink); line-height: 1.75; }
```

### 搬的时候必须一起带走的三件事

1. **字体分工**（第三节）。只搬色板不搬字体分工，出来会是「配色不错但字很脏」。
2. **`--sheet-pad` 这个「同宽的单一来源」**（第四节）。
   正文、章节头、章尾、`.chapter-toolbar` 共用它，才有「四条边线重合」。
   若改成四份各自写死的 padding，以后必然错开，而且是肉眼不易发现的几像素错开。
3. **换页过渡的底衬检查**（第九节）。淡出前先问：透明之后露出什么？

> `--sidebar-hold` 镜像让位**不是必搬项**：它只在「没有两侧翻页区」的窄屏区间还起作用。
> 若新项目不做三栏翻页，可以整套照搬（那样正文永远视口居中）；
> 若做三栏，就要接受 5.2 里那条取舍——**正文更宽，但整组内容略偏右**。

### 需要按项目重做、不要照搬的

- **领域色语义**：本站四色对应四个知识领域。新项目若没有「领域」概念，
  应改成按业务语义分配（如 成功/警告/信息/品牌）。
- **断点具体数值**：`2000 / 1700 / 1300 / 1128 / 980 / 760` 都是被本站的具体内容
  （侧栏 268px、翻页区 150px、正文栏可读下限 720px）算出来的。
  换个侧栏宽度或翻页区宽度就要重算——别把它们当成「业界标准值」。
- **图解动效**：`dm-*` 这套依赖「手写 SVG + 构建期插件」。若新项目用位图或
  第三方图表库，需要另做方案（思路可以照搬：错峰淡入表达顺序、彗星表达流向）。

---

## 十三、「不要做」清单（13 条）

| # | 不要 | 因为 |
| --- | --- | --- |
| 1 | 在 `font-family` 里写 `SimSun` / `宋体` | Windows 上发虚，有一圈毛边 |
| 2 | 给 `body` 用深色底 | 换页淡出时会露出来 → 灰屏 |
| 3 | 换页动画位移超过 ~10px | MPA 是「演完再跳」，幅度大像卡住 |
| 4 | 在浅色 `--paper` 上叠深色面板 | 整站的层次靠纸的三级深浅，不是靠明暗对比 |
| 5 | 阴影用纯黑 | 纸面会有塑料感；用 `--shadow`（墨色 + 极低透明度） |
| 6 | 去掉 `.main { min-width: 0 }` | 长代码行会把整页撑到横向溢出 |
| 7 | 让正文栏在窄屏继续居中 | 镜像留白会把它挤到 185px，一行不到 11 字 |
| 8 | 图解在小屏等比缩放 | 9px 的字缩到 4.7px，等于没有图 |
| 9 | 把 `.page-rail` 在 <1300px 上硬塞 | 正文栏会被压到不好读；窄屏本来就有章尾 `.pager` |
| 10 | 新增动效不写 `prefers-reduced-motion` | 晕动症用户无法使用 |
| 11 | 只改 `--sheet-pad` 而不改 `.chapter-toolbar` 的负 margin | 正文与工具栏会错开；两次体检脚本都会报红 |
| 12 | 把 `.page-rail` 写成 `.sheet` 的子节点 | 三栏 flex 会静默塌回单栏，且不报错 |
| 13 | 把关于页出口区的竖卡排到三列 | 侧栏占位后纸面只剩 600～700px，说明文案被压成「一列一个字」；第三张改横条 |

---

## 十四、相关文档

| 文档 | 内容 |
| --- | --- |
| `wiki/schema.md` | 内容侧契约：frontmatter、正文骨架、**图解 / 代码的完整规范**、术语表 |
| `wiki/README.md` | 知识库入口：三层结构、三个操作、**不变量清单** |
| `README.md` | 工程侧：本地开发、构建、两个容易踩的坑、部署 |
| `scripts/measure.mjs` | 把本文的版面主张（居中、留白、栏宽）变成可执行的断言 |
| `src/styles/global.css` | 本文的实现。23 个分节，注释里保留了每次改动的**原因** |
